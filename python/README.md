RacingDashCam (python package)

This directory contains the Python package for RacingDashCam.

For the full project README and documentation, see the repository root `../README.md`.

This file exists so packaging tools can include a project long-description when building/installing from the `python/` folder.
